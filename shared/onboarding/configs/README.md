# QuinnAI Deployment Onboarding Docs

These files are the agent-facing rules shipped into deployed QuinnAI orgs.

- `CLAUDE.md` is for worker architecture rules and system constraints.
- `AGENTS.md` is for worker behavior expectations and operating norms.

Project contributor guidance lives at the repo root (`CLAUDE.md`, `AGENTS.md`).
