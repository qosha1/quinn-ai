[pytest]
testpaths = tests cli/tests wrkr/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Ignore backend (has separate pytest config, runs in CI via test-backend job)
addopts = --ignore=backend
